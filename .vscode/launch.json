{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build and Run WiiFunkin",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/out/${workspaceFolderBasename}.elf",
      "preLaunchTask": "Run WiiFunkin",
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",
      "stopAtEntry": false,
      "environment": []
    }
  ]
}
